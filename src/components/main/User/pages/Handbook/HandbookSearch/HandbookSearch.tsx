import './HandbookSearch.scss';

const HandbookSearch = () => {

    const handleChange = e => {
        
    }

    return (
        <input type="text" className='handbook-searchbar' placeholder='Поиск...' onChange={handleChange}/>
    )
};

export default HandbookSearch;
