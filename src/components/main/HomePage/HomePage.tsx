import { FormConstructor } from '..';
import './HomePage.scss';

const HomePage = () => {
    return (
        <section className='main'>
            <FormConstructor />
        </section>
    );
};

export default HomePage;
