import { dragableIcon } from '../../../assets';
import './FormElementDragButton.scss';

const FormElementDragButton = () => {
    return (
        <img className='form-element-drag-button' src={dragableIcon} alt='' />
    );
};

export default FormElementDragButton;
